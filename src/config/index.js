const WEB_API = "https://raffle.upcomingnft.net/v1/";
const AUTH_PATH = "https://raffle.upcomingnft.net/auth/";
const SOCKET_API = "ws://localhost:8080";
const SECRET_KEY = "raffle";

const CONFIG = {
    WEB_API,
    AUTH_PATH,
    SOCKET_API,
    SECRET_KEY,
};

export default CONFIG;
